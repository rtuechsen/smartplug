/**
 * The intervall in milliseconds in which the frontend will send a request to the backend in order to keep the session active.
 * 
 * This request will ony be sent if user input was registered.
 */
export const SESSION_UPDATE_INTERVAL_MS: number = 2000;
