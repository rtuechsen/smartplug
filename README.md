
## Backend

Run `pip install -r ./requirements.txt` to install dependencies after cloning this project.  

Run `bash deploy.sh` to deploy using nginx.  

## Frontend

build using: `npm run build`  
debug using: `npm run dev`  

Needs the Extension `ESLint` for in-code-linting in VSCode.  